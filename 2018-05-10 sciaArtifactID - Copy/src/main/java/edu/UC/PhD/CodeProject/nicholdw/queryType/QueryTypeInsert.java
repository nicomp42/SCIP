package edu.UC.PhD.CodeProject.nicholdw.queryType;

/**
 * A query that adds records to a table. May also be an "Append" query in some dialects.
 * @author nicomp
 *
 */
public class QueryTypeInsert extends QueryType {
	public QueryTypeInsert() {
		super("Insert");
	}
}
