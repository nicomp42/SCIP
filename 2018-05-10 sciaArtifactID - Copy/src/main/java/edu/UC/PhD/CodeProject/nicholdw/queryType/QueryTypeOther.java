package edu.UC.PhD.CodeProject.nicholdw.queryType;

/**
 * A query type we don't know to handle.
 * @author nicomp
 *
 */
public class QueryTypeOther extends QueryType {
	/**
	 * You can give it a type, but this is a query type we don't know how to handle
	 * @param queryType The type of this query
	 */
	public QueryTypeOther(String queryType) {
		super(queryType);
	}
}
